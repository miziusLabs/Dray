# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Cargo build trees can contain thousands of files. Besides being generated,
# leaving nested targets visible makes the pre-turn Git snapshot especially
# expensive on Windows.
target/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

docs
# Staged from vscode-material-icons on every Vite start — see vite.config.ts
apps/desktop/public/file-icons

# Browser-only harness for looking at panel states without a session or gh.
# Local tooling, not part of any build — see the PR panel notes in CLAUDE.md.
apps/desktop/demo

*.tsbuildinfo
.gstack/

# Release runbook. Local only — it names hosts and checks that are
# operator business, not the public repo's.
apps/desktop/RELEASING.md
